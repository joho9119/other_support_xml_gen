[project]
name = "wordparser"
version = "0.1.0"
description = "Parses Other Support Common Form to XML for upload into SciENcv."
requires-python = ">=3.14"
dependencies = [
    "python-docx==1.2.0",
    "requests",
]
